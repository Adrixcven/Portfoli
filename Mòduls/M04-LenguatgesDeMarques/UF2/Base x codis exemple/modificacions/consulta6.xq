(:Insertem un nou element a l'inici del document amb l'adreça de la llibreria:)
(:"as first into" indica que la inserció ha de ser al principi del node que el segueix. Existeix "as last into" perque la inserció sigui al final, el mateix que fa into :)

insert node <adreca>C/ LLibre, 2</adreca> as first into doc("../bookstore.xml")/bookstore